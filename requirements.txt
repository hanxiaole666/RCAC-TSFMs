# RCAC reproduction environment
# Verified on: Windows 10, Python 3.11.5, PyTorch 2.2.1+cu121, RTX 2060 6GB
# Total install size ~4 GB (PyTorch CUDA dominates)

torch>=2.0                # TSFM inference (2.2.1 verified)
transformers>=4.32        # Kronos tokenizer / Chronos pipeline
einops                    # Kronos model internals
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
scikit-learn>=1.3         # CQR / EnbPI baselines
arch>=6.0                 # GARCH-t baseline
akshare>=1.12             # market data (CSI 300/500, 600519)
huggingface_hub           # model downloads (mirror configured in scripts)
